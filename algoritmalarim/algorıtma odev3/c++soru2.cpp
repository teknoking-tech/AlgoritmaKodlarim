#include <iostream>

/* furkan yorgun 2020717019 */
int main(int argc, char** argv) {
	float x,y;
	
	printf("x degerini giriniz:");
	     scanf("%f",&x);
	     
	     if (x<0) printf("x<0 oldugunda y sayisinin degeri %.2f\n:", y=1);
	     
	         if (x>=0 & x<2) printf("0<=x<2 oldugunda y sayisinin degeri: %2.f", y=x*x-2*x );
	         
		          if (x>=2 & x<4) printf("2<=x<4 oldugunda y sayisinin degeri: %.2f" ,y=-x*x-(x/2)+x);
		          
		                if (x>=4) printf("4<=x oldugunda y sayisinin degeri %.2f\n:" ,y=x);
	
	
	return 0;
}
